import { MutateFunction, MutationKey, QueryKey } from '@tanstack/react-query';

export const useMutationData = (mutationKey: MutationKey, mutationFn: MutateFunction<any, any>, queryKey: QueryKey) => {};
